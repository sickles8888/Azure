#
利用Json脚本管理Azure资源
作为学习和整理使用

注意： 所有脚本建议在Azure China上使用测试

环境要求：
规划Azure的架构环境时，已经提前规划好虚拟网络，并依照不同的业务创建好了对应的子网，并对子网通过NSG的做访问控制。
为了安全所有虚拟机均不直接分配公网IP地址，仅仅允许通过跳板机或是企业内部网络远程登录访问，企业与Azure间通过VPN或是ER专线进行连接。
需要用户简单的修改脚本。根据自己环境调整Vnet所在的资源组的名字、Vnet的名字、子网的名字。

创建出来的VM虚拟机需要用户自行调整是否开启监控，网卡管理NSG，配置负载均衡等后续的操作。
